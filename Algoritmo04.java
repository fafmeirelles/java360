public class Algoritmo04 {
    public void main(){
        /*Estrutura de seleção simples ou composta
        int x = 10.
        if(x>10){
            IO.println("Número é maior ou igual a 10");
        }
        int y = 20;
        if(y==1)
            IO.println("o numero e igual a 1");
               else{
        IO.println("o numero é maior ou igual a 1");
    }
    int z = 5;
    if(z<10){
        IO.println("numero é menor que 10");
    }
    int n = integer.parseInt(IO.readln("Entre com o número"));
    if(n>0){
        IO.println("O número é positivio");
    }else if(n<0>){
        IO.println("O número é negativo");
    }else{
        IO.println("O número é nulo");
    }  
    //operador ternário
    int a = 20
    String resultado = (a>10)? "maior que 10" : "menor ou igual a 10";
    IO.println(resultado); */
    
    //operador teernário com entrada de dados
    // estrutura de selação múltipla
    int numAluno = integer.parseInt(IO.readln("Entre ciom o número de 1 a 4"));
    switch(numAula){
        case 1:
            IO.println("Segunda-feira");
            break;
        case 2:
            IO.println("Terça-feira");
            break;
        case 3:
            IO.println("Quarta-feira");
            break;
        case 4:
            IO.println("Qinta-feira");
            break;
        default:
            IO.println("Número inválido");
    }
    }  
}

  
